

- (NodeJs 설치) => JSON Server 생성 => 서버에서 제공하는 데이터(db.json)
 => JSON 서버 실행

 npm init -y
 npm install json-server --save-dev
 json-server --watch db.json 
 npm start server 


 동기 프로그래밍 => 작업이 차례로 실행되며 이전 작업이 완료될 때까지 다음 작업이 진행될 수 없다.
 비동기 프로그래밍 => 임의의 순서로 또는 동시에 작업이 실행되는 것처럼 실행되는 형태

자바스크립트는 런타임(브라우저나 NodeJs)에서 싱글 스레드로 동작

싱글  스레드로 동작하지만, 콜백, 프로미스, 어싱크 어웨이트 방법

xhr 콜백 함수, 이벤트
Promise 객체 (ES6에서 사용) : 콜백대신 사용하는 방법
              비동기 작업이 완료되면 결과를 반환하는 객체

async, await 키워드 
프로미스 객체를 사용하는 비동기 작업을 동기적으로 처리하는 키워드
